# Minimal deps for the scheduled ETL job (fast CI installs, no ML libs)
aiohttp>=3.9
tenacity>=8.2
pandas>=2.1
numpy>=1.26
SQLAlchemy>=2.0
psycopg2-binary>=2.9
python-dotenv>=1.0
fastapi>=0.109        # services/data_access.py raises HTTPException; also needed by CI's pytest run
